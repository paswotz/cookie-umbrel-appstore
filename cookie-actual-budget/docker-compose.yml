services:
  app_proxy:
    environment:
      APP_HOST: cookie-actual-budget_web_1
      APP_PORT: 5006
      PROXY_AUTH_ADD: "false"

  web:
    image: actualbudget/actual-server:latest
    user: 1000:1000
    restart: on-failure
    environment:
      - ACTUAL_PORT=5006
      - ACTUAL_TRUSTED_PROXIES=umbrel.local,umbrel
    volumes:
      - ${APP_DATA_DIR}/data:/data
